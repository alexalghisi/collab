# Assets

This directory holds the app's visual assets:

- `icon.png` — application icon used by Expo (iOS, Android adaptive icon, web favicon) and the desktop packagers.
- `demo.gif` — recording of a local run referenced at the top of the root `README.md`: dashboard, calendar, a live call, the shared C++ editor writing `date.out`, in-call chat, and the host tools.
